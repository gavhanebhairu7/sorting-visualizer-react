import React from "react"
import ReactDOM from "react-dom/client"
import "./index.css"
import Sorting from "./Sorting_visualizer"
import Header from "./components/Header"
ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Sorting />
  </React.StrictMode>
)
